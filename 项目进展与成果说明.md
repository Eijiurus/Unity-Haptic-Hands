# 项目进展与成果说明

**文档性质**：基于当前仓库代码、`README.md` 与 Git 提交历史整理的阶段性说明，用于汇报毕业设计进展与可展示成果。  
**工程定位**：Unity 2022.3 LTS 上的「基于数据手套的虚拟手交互与触觉反馈」原型系统（本科毕业设计方向）。  
**生成日期**：2026-04-18  

---

## 一、项目目标（要解决什么问题）

在 Windows 上搭建一套可演示的闭环：**真实手指弯曲**驱动 **Rigged Hand 骨骼**，**手腕姿态与位移**来自 **BLE 惯性传感器**，场景中通过 **Trigger / 刚体** 完成触摸与抓取；触觉侧提供 **HID 板直连测试脚本**（及 README 中说明的 Arduino + DRV2605 扩展路径），便于验证马达与协议。

> 说明：`.cursorrules` 中曾提到 XR Interaction Toolkit；当前 `Packages/manifest.json` **未**引入 XRI，交互由自定义脚本与 Physics 完成，与主 `README.md` 描述一致。

---

## 二、当前整体进展（可概括为「可运行、可演示、可排障」）

| 维度 | 状态 |
|------|------|
| Unity 工程 | **2022.3.62f3** LTS，场景 `SampleScene`、左手 Prefab、材质与 FBX 资源齐备 |
| 手套数据通路 | **串口 → Python `SensorBridge.py` → UDP → `GloveDataReceiver`**，五指弯曲驱动 `DataGloveHandDriver` |
| 手腕 / 位移 | **WitMotion WT9011 BLE** → `WitMotionConnector` + `DeviceModel` → 手腕旋转与“重力估计 + 启动标定 + 冻结窗口 + 弹簧回中”的受限相对位移 |
| 场景交互 | **非 XR**：指尖 Trigger、`HandInteractionRig` 捏合/握拳/距离捏合、`TouchableObject` 高亮与 `UnityEvent` |
| 无硬件调试 | **键盘模拟手套**（1–5、Space）、**Scene 视图键盘微调**（`SceneViewHandKeyboardBridge`） |
| 触觉验证 | 根目录 **`test_hid.py` / `test_vibrate.py`** 针对 VID `0x674E` / PID `0x000A` 的 HID 双路板 |
| 文档 | 根目录 **`README.md`** 覆盖架构、目录、脚本职责、启动步骤与常见问题 |

**结论**：核心数据链路与虚拟手驱动、BLE 手腕、抓取交互与离线调试工具链已落地；触觉在游戏内与固件的深度联调取决于实际硬件协议，当前以 **HID 脚本验证** 与 **`TouchableObject` 事件扩展** 为主。

---

## 三、成果展示（可按演示顺序对外说明）

### 1. 虚拟手与数据手套联动

- **成果**：五指弯曲实时映射到骨骼旋转；数据格式为手套 ASCII 行（分号结尾、11 个逗号分隔值，前 5 为弯曲通道），经 Python 去分号后以 UDP 整行送入 Unity。
- **对应实现**：`SensorBridge.py`、`GloveDataReceiver.cs`、`DataGloveHandDriver.cs`。

### 2. WitMotion 手腕跟踪

- **成果**：自动扫描名称含 **WT** 的 BLE 设备并连接；解析后用于手腕姿态，并支持更稳健的相对位置控制。
- **本轮进展**：`DataGloveHandDriver.cs` 已将位置部分从“依赖欧拉角补偿的自由积分”重构为 **慢速低通估计重力 + 启动静止标定 + 转动冻结窗口 + 弹簧回中** 的模型，目标从“纯 IMU 位置积分”调整为“优先压制转腕假位移，同时保留可感知的水平相对偏移”，更适合答辩演示和场景交互。
- **对应实现**：`WitMotionConnector.cs`、`Assets/Scripts/Bluetooth/` 下 `BleApi`、`BlueConnector`、`BlueScanner`，`Device/DeviceModel.cs`、`DevicesManager.cs`、`DataGloveHandDriver.cs`。

### 3. 场景内触摸与抓取

- **成果**：指尖球形 Trigger 检测可触摸物体；支持 **弯曲阈值捏取**、**拇指食指距离捏合**、**握拳抓取**、**G 键无手套捏取**；可选指尖物理碰撞球减轻穿模；可吸附刚体到指尖或手掌根。
- **对应实现**：`HandInteractionRig.cs`、`FingerTipRelay.cs`、`TouchableObject.cs`。

### 4. 编辑器与场景工作流

- **成果**：菜单 **Tools → Setup Rigged Hand Prefab** 一键从 FBX 生成左手 Prefab 并布置场景默认项；`HandSceneSetup` 使主摄像机跟随手部包围盒。
- **对应实现**：`Assets/Editor/RiggedHandPrefabSetup.cs`、`HandSceneSetup.cs`。

### 5. 触觉硬件侧验证

- **成果**：不依赖 Unity 即可对 HID 板发 **0xAA**（库振动）与 **0xBB**（遍历 DRV2605 效果 ID）命令，用于双路马达快速冒烟测试。
- **对应实现**：`test_hid.py`、`test_vibrate.py`。

---

## 四、仓库演进（Git 历史反映的里程碑）

下列为近期提交主题摘要，体现从「串口直连」到「多通道融合与文档完善」的迭代路径：

1. **Initial commit**：数据手套虚拟手系统基线。  
2. **UDP 桥接**：手套改经 `SensorBridge.py` 串口转 UDP；`GloveDataReceiver` 重写；移除未用控制器；README 同步数据流。  
3. **WitMotion BLE**：手腕 BLE 跟踪与连接修复。  
4. **交互**：触摸/抓取与 SampleScene 布局整理。  
5. **融合调优**：WitMotion 位置控制优先与场景交互调参。  
6. **漂移抑制**：加入重力补偿、角速度门控、静止清速，降低 WT9011 转腕导致的假位移。  
7. **位置重构**：将 WT9011 位置链路进一步改为重力低通估计、启动静止标定、转动冻结窗口与弹簧回中模型，强调稳定的受限相对位移而非自由积分。  
8. **文档与 HID**：抓取行为说明、调试补充；新增 HID/振动测试脚本并更新 README。

---

## 五、目录与交付物清单（便于检查完整性）

- **Unity 脚本**：`Assets/Scripts/` 下接收、驱动、BLE、设备解析、交互共 **11 个 C# 文件**（含子文件夹）。  
- **编辑器工具**：`Assets/Editor/RiggedHandPrefabSetup.cs`、`SceneViewHandKeyboardBridge.cs`。  
- **Python**：`Assets/Scripts/SensorBridge.py`；根目录 `test_hid.py`、`test_vibrate.py`。  
- **资源**：`Assets/Models/Rigged Hand.fbx`、`Assets/Prefabs/Hands/LeftHand.prefab`、`Assets/Materials/HandSkin.mat`、`Assets/Scenes/SampleScene.unity`。  
- **说明文档**：`README.md`（技术手册）；**本文件**（进展与成果汇报用）。

---

## 六、与「完全成品」之间的差距（诚实边界，便于答辩口径）

- **XR**：当前为桌面/非 XR 演示形态，未集成 XR Interaction Toolkit。  
- **触觉闭环**：Unity 内可通过 `TouchableObject` 的 `UnityEvent` 接串口/USB；与 HID 板的完整游戏内闭环需按固件协议对接（VID/PID 与报文可按实机修改）。  
- **Arduino + DRV2605**：README 列为可选路径；具体固件与串口协议以实际硬件为准。  
- **WT9011 位移边界**：当前方案强调“相对位移可演示、转动假位移更可控”，并非严格意义上的无漂移绝对 3D 定位；Inspector 中的 `positionAxisWeight`、`gravityEstimateFilterSpeed`、`positionFreezeDuration`、`wristPositionSpring` 等参数仍需要结合佩戴方式和演示动作继续实测调优。  
- **资源占用**：`BlueScanner` / `BlueConnector` 与 `WitMotionConnector` 并存时注意 BLE 资源，README 已提示。

---

## 七、维护建议

架构、端口、数据格式、WT 位置估计策略或抓取逻辑变更时，请同步更新 **`README.md`** 相关章节；若对外汇报材料与本文件不一致，以 **`README.md` + 当前工程 Inspector 配置** 为准。
